package com.project.Clinical_System.entity;

//It ensures that a variable can only take one of the predefined values.

public enum AppointmentStatus {
    SCHEDULED,
    COMPLETED,
    CANCELED,
    RESCHEDULED
}
